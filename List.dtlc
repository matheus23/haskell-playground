( λ List : Type → Type
. λ foldList : (∀ a . List a → ∀ b . (a → b → b) → b → b)
--. λ mkList : (∀ a . (∀ b . (a → b → b) → b → b) → List a)
.
3
)

-- List
( λ a : Type . (∀ b . (a → b → b) → b → b) )

-- foldList
( λ a : Type
. λ list : (∀ b . (a → b → b) → b → b)
  . list )
{-
-- mkList
( λ a : Type
. λ fold : (∀ b . (a → b → b) → b → b)
  . fold )
-}
